##########################################################################
#                НЕ УДАЛЯЙТЕ И НЕ РЕДАКТИРУЙТЕ ЭТОТ ФАЙЛ                 #
# Этот файл сгенерирован автоматически и используется для запуска тестов #
#                                                                        #
#                    DO NOT DELETE OR EDIT THIS FILE                     #
#     This file is automatically generated and used to run tests         #
##########################################################################

name: hexlet-check

on:
  push:
    branches:
      - '**'
    tags:
      - '**'
env:
  TEST_ENV: ${{ secrets.SESSION_KEY }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Test SESSION_KEY: ${{ secrets.SESSION_KEY }}"
          echo "Test TEST_ENV: ${{ env.TEST_ENV }}"
      
      - name: Make envfile
        uses: SpicyPizza/create-envfile@v1
        with:
          envkey_SESSION_KEY: "0123456789ABCDEF0123456789ABCDEF"
          envkey_ROLLBAR_KEY: "0123456789ABCDEF0123456789ABCDEF"
          envkey_SECRET_KEY: ${{ secrets.SESSION_KEY }}
          directory: .
          file_name: .env

      - uses: actions/checkout@v2
          
      - name: Hexlet project check
        uses: hexlet/project-action@release
        with:
          hexlet-id: ${{ secrets.HEXLET_ID }}
          

